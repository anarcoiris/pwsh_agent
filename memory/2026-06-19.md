# 2026-06-19 — Multi-GPU deploy + KV Q4 context ladder

## Multi-GPU stack (verified)

| GPU | Container | Port | Model | Role |
|-----|-----------|------|-------|------|
| GTX 1080 (0) | ollama-coder | 11436 | qwen2.5-coder:7b-instruct | VALIDATE + EXECUTE |
| GTX 1070 (1) | ollama-planner | 11434 | vibethinker:3b | PLAN + EVALUATE |
| GTX 1070 (2) | ollama-intake | 11435 | chat-analyzer | INTAKE |

- `.env`: `GPU_DEVICE_CODER=0`, `PLANNER=1`, `INTAKE=2`
- Shared volume `ollama_models`; `setup-models.ps1 -ContainerName ollama-coder` OK
- `verify-multi-gpu.ps1` warm-up OK (~10s / ~17s / ~37s cold intake)

## Model roles (do not confuse)

- **chat-analyzer** = `FROM qwen2.5:7b` + structured analysis system prompt (INTAKE)
- **qwen2.5:3b** = separate 3B tag; benchmark profiles only (`config.models/profiles.yaml`); **not** intake; pull optional/irrelevant for production

## KV cache policy

- Compose (`docker-compose.multi-gpu.yml` + legacy `docker-compose.yml`): `OLLAMA_KV_CACHE_TYPE=q4_0`
- Old `ollama-code` inspect (2026-06-18) had Q8 — recreate containers after env change
- Q4 KV ~4× smaller than FP16 → headroom for longer `num_ctx` on 8 GB Pascal
- RAG/embeddings stay on CPU (FAISS/Jaccard + feeds)

## Context targets (apply only after vram-probe)

| Role | Applied cap | Notes |
|------|-------------|-------|
| chat-analyzer | 8192 | intake — keep (low priority) |
| vibethinker | **32768** | planner — idle probe OK @ ~5.5 GB |
| qwen-coder | **16384** | idle probe OK @ ~5.7 GB (prior timeout was hygiene contention) |

Probe log: `Ollama/docker/logs/vram-probe-20260619.json`

**Run probes when no hygiene/agent jobs are active** — parallel Ollama load invalidates latency/OOM signals.

## Agent canon

Full normative design: `docs/AGENT_CANON.md` (Optioneer, MVF-first, goal lock).

## pwsh_agent code (same session)

- Commit `d5e620b`: 5-phase pipeline, endpoint routing, roadmap_validator, task_graph parallel
- Endpoints in `config.yaml` + `exploration-kernel/protocols/model_routing.yaml`

## setup-models note

- Failed pull `qwen2.5:3b` = DNS in container + optional model; does not block pipeline
- Use `-SkipPullOnly` or optional flag in registry

## Plans sync (MVF + Agencia + GPU + T1)

- Índice maestro: `docs/plans/MVF_AGENCY_ROADMAP.md` — incluye R1c (`gpu_allocation_plan.md`) y T1 (`tool_loop_plan.md`)
- Orden: `0/0b → 0a R1c → 0c T1 → R2 → R4 → loop_restrictions+T1 → R7 → Q4 → R5`
- Baseline daemon 2026-06-19: wizard `"hello_game template"` incorrecto; usar `enqueue_from_template("hello_game")`

## R1c + R2 + R4 aplicados (sesión actual)

- **R1c v2:** coder en GTX 1080 `:11436`; planner 1070 `:11434`; intake 1070 `:11435`
- Docker recreado: `docker compose -f docker-compose.multi-gpu.yml up -d --force-recreate`
- **R2:** `mvf_autonomous` + sesión `q_<job_id>`
- **R4:** `core/mvf_validator.py`, gate MISSION_COMPLETE, plantilla hello_game con bloque `mvf`
- **HelloGame baseline:** existe `hello_game/` (JS helloworld) — canon exige `HelloGame/` (Python + pytest)

## R4b + hardening agencia (post-incidente ejemplos-texto)

- **Incidente:** misión consola `ejemplos-texto/` — falso éxito (host_exec roto, append_note sin disco, hygiene REF-001).
- **R4b:** `mvf_exit_blocked` en todas rutas salida; `max_steps` ya no emite MISSION_COMPLETED si MVF falla.
- **MVF:** checks `dir_exists`, `dir_count`; auto-derive para misiones bulk N archivos.
- **delivery_probe:** CPU post-`append_note` advierte claims sin evidencia.
- **Aislamiento:** `sweep_loop` usa `get_background_agent()`; consola solo busy-flag.
- **Guards:** bulk `host_exec` bloqueado en `code_build`; hygiene fence en RAG/playbooks.
- **Playbook:** `knowledge/skills/code_build_delivery.md`
- **Tests:** `test_delivery_probe.py` (18/18 en suite R4b)
