# 2026-06-10 — Session log

## Work completed (context/monitoring/retry stack)

- **Phase 0 observability**: token telemetry in `llm_audit.jsonl` (`prompt_eval_count`, `eval_count`, `ctx_saturation` vs `num_ctx`), native vs parsed tool-call counts, parser fallback paths. Modes `full|meta|off` (`config.yaml agent.llm_audit`, currently `full` for validation). Viewer `tools_dev/llm_audit_view.py`; console `audit` → `llm` sub-view.
- **Phase 1 context diet**: specialists get a minimal pinned prompt (no AGENTS/SOUL/TOOLS md); LEAD keeps AGENTS+SOUL + one-line roster; schema injection and `tools=` reordered by current plan step (`priority_tools`).
- **Phase 2 trial-and-error**: verbatim deduped error feedback (~1500 chars, `Line |` markers kept) for run_script/host_exec (incl. PS non-terminating errors at exit 0); per-step attempt counts + error signatures persisted; repeat-error nudge; 8-attempt cap → step `BLOCKED` + strategy-change directive; success gating (exit code 0) for step done + handoff complete.
- **Phase 3 verbose content**: `http_get` saves FULL body to session artifact before truncation (preview 2500 chars), `facts.web.last_page/pages`, web specialist shares `grep_file`/`read_file` (`SHARED_TOOLS`), `try_http_login` gated until fetch_page/artifact exists. Playbook `knowledge/tools/http_get.md`.

## Tests

- New: `test_llm_audit.py`, `test_retry_executor.py`, `test_http_get_artifact.py`.
- Updated: `test_prompt_pack_budgets.py` (minimal specialist prompt), `test_tool_schemas.py` (priority tools), `test_web_auth_planner.py` (verdict-gated login terminal).
- Full suite: 296 passed; 1 pre-existing environmental failure (`test_artifacts.py::test_find_file_prefers_output_reports` — too many `output/report_*.md` from old sessions push the knowledge file out of the capped find_file list).

## Operator notes

- Ollama context truth: `/api/chat` is stateless — we send history each turn; effective window is `num_ctx` (8192), not the model card's 128k. Watch `ctx_saturation` in `audit → llm`; >= 0.95 means silent input truncation.
- After validation, flip `agent.llm_audit` to `meta` (full mode re-logs the whole message array each step).
- Validation missions to run live: router login (fetch → grep artifact → gated login) and the ping-sweep script (should converge through PS errors within 8 attempts).

## Validation (live, same session)

- **Router** (`20260610_015312`): fetch → 153KB artifact on disk → `facts.web` with `xmlobj` keyword → no premature login (gate held). PASS.
- **Ping sweep** (`20260610_015608`): converged through `@echo off` batch/PowerShell confusion in 2 fix iterations (verbatim stderr fed back, incl. Spanish locale errors); final script exit 0, `ping_results.log` written. PASS.
- **Headroom**: worst `ctx_saturation` 0.742 < 0.95 — no truncation; `native_tool_calls` 0 everywhere (text parser carries all calls). PASS.
- Live fix during validation: deterministic URL correction `_correct_web_target_arg` — 7B drops IP octets when re-typing URLs (`http://168.1.1`) and won't recover from timeouts alone.
- One transient Ollama CUDA 500 crash (`ollama-code` container) on the first attempt; recovered by itself on retry.
- `agent.llm_audit` flipped to `meta` after validation.
- Validation harness kept at `tools_dev/validate_mission.py` (missions: `router`, `pingsweep`, `continue`).

## Next session

- Web auth Phase 3: custom XML/raw POST + `SID` cookie jar in `try_http_login` ([web_auth_html_pipeline_plan.md](../docs/plans/web_auth_html_pipeline_plan.md)) — the router sets `SID` cookie on fetch; artifact + grep now provide the protocol evidence.
- Consider raising chat-mode step budget or auto-continuation for multi-iteration script fixes (12-step turn cap ended the sweep turn mid-fix; `continue` finished it).
